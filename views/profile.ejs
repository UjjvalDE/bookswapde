<!-- views/profile.ejs -->
<%- include('partials/header', { pageTitle: 'Profile' , user: user }) %>

    <div class="profile-container">
        <div class="profile-wrapper">
            <div class="profile-header">
                <h1>My Profile</h1>
                <p>Welcome to your Book Swap profile</p>
            </div>

            <div class="profile-content">
                <div id="profileError" class="alert alert-danger" style="display: none;"></div>

                <div class="profile-grid">
                    <div class="profile-image-section">
                        <div class="profile-image-wrapper">
                            <img id="profileImage"
                                src="https://media-hosting.imagekit.io//0f593923344a43d6/cat.jpeg?Expires=1835982747&Key-Pair-Id=K2ZIVPTIP2VGHC&Signature=Av9Zf65AAAmAWwhKtkfpGcdsJtCwx4~rOLehcJlA9jxxiFPHaVQDNlydD7q9wrTPzHKR4GH69uaGDWw2HbpPIbuIsLTW9252rdf7oRDkE-vxclhCDT~cwy1KIdK7O2uthxb77xToUPOCHLD-pgyCmfFo~sZhjpyFno~17GV7-xYr1a~iQSkgi-SUldLOgj9DIbANwtgRnqF9ve7YyfeqOPpsyH89sFgqcAb7vbaEmvQi6a8oX9En9Fx0macHeaiuat8BJrtLfziu8Gu2gbHDU2~uaCTbyynCKrngudIlnp9AYvILbzYfPsSIjQj5pwvBa347Uf9-~JV~c3U~B75rYg__"
                                alt="Profile Cartoon">
                            <div class="profile-image-overlay">
                                <i class="fas fa-camera"></i>
                            </div>
                        </div>
                        <p class="profile-image-caption">Cartoon by Book Swap</p>
                    </div>

                    <div class="profile-details">
                        <div class="profile-section">
                            <h2>Personal Details</h2>
                            <div class="details-grid">
                                <div class="detail-item">
                                    <i class="fas fa-user"></i>
                                    <div class="detail-content">
                                        <span class="detail-label">Name</span>
                                        <span id="userName" class="detail-value">Loading...</span>
                                    </div>
                                </div>
                                <div class="detail-item">
                                    <i class="fas fa-phone"></i>
                                    <div class="detail-content">
                                        <span class="detail-label">Phone</span>
                                        <span id="userPhone" class="detail-value">Loading...</span>
                                    </div>
                                </div>
                                <div class="detail-item">
                                    <i class="fas fa-map-marker-alt"></i>
                                    <div class="detail-content">
                                        <span class="detail-label">Address</span>
                                        <span id="userAddress" class="detail-value">Loading...</span>
                                    </div>
                                </div>
                                <div class="detail-item">
                                    <i class="fas fa-envelope"></i>
                                    <div class="detail-content">
                                        <span class="detail-label">Email</span>
                                        <span id="userEmail" class="detail-value">Loading...</span>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="profile-section">
                            <h2>Reading Interests</h2>
                            <div class="interests-wrapper">
                                <div id="interestedBooks" class="interests-grid">
                                    <!-- Book categories will be dynamically populated here -->
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="profile-actions">
                    <a href="/editProfile" class="btn btn-primary">
                        <span>Edit Profile</span>
                        <i class="fas fa-edit"></i>
                    </a>
                </div>
            </div>
        </div>
    </div>

    <style>
        :root {
            --primary: #EED47E;
            --secondary: #2C3E50;
            --accent: #34495E;
            --light: #F8F9FA;
            --dark: #1A1A1A;
            --white: #FFFFFF;
            --text: #2C3E50;
            --gradient: linear-gradient(135deg, #EED47E, #F4E5A3);
            --shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            --card-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }

        .profile-container {
            min-height: 100vh;
            padding: 4rem 2rem;
            background: var(--light);
        }

        .profile-wrapper {
            max-width: 1200px;
            margin: 0 auto;
        }

        .profile-header {
            text-align: center;
            margin-bottom: 3rem;
        }

        .profile-header h1 {
            font-size: 2.5rem;
            font-weight: 800;
            color: var(--secondary);
            margin-bottom: 0.5rem;
            background: linear-gradient(45deg, var(--secondary), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .profile-header p {
            color: var(--accent);
            font-size: 1.1rem;
            opacity: 0.8;
        }

        .profile-content {
            background: var(--white);
            border-radius: 30px;
            padding: 3rem;
            box-shadow: var(--card-shadow);
        }

        .profile-grid {
            display: grid;
            grid-template-columns: 300px 1fr;
            gap: 3rem;
            margin-bottom: 2rem;
        }

        .profile-image-section {
            text-align: center;
        }

        .profile-image-wrapper {
            position: relative;
            width: 250px;
            height: 250px;
            margin: 0 auto 1rem;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: var(--shadow);
        }

        .profile-image-wrapper img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        .profile-image-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .profile-image-overlay i {
            color: var(--white);
            font-size: 2rem;
        }

        .profile-image-wrapper:hover img {
            transform: scale(1.05);
        }

        .profile-image-wrapper:hover .profile-image-overlay {
            opacity: 1;
        }

        .profile-image-caption {
            color: var(--accent);
            font-size: 0.9rem;
            opacity: 0.8;
        }

        .profile-section {
            margin-bottom: 2.5rem;
        }

        .profile-section h2 {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--secondary);
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .profile-section h2::before {
            content: '';
            width: 4px;
            height: 24px;
            background: var(--primary);
            border-radius: 2px;
        }

        .details-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 1.5rem;
        }

        .detail-item {
            display: flex;
            align-items: flex-start;
            gap: 1rem;
            padding: 1rem;
            background: var(--light);
            border-radius: 12px;
            transition: transform 0.3s ease;
        }

        .detail-item:hover {
            transform: translateY(-2px);
        }

        .detail-item i {
            color: var(--primary);
            font-size: 1.25rem;
        }

        .detail-content {
            display: flex;
            flex-direction: column;
            gap: 0.25rem;
        }

        .detail-label {
            font-size: 0.875rem;
            color: var(--accent);
            opacity: 0.8;
        }

        .detail-value {
            font-size: 1rem;
            color: var(--secondary);
            font-weight: 500;
        }

        .interests-wrapper {
            background: var(--light);
            border-radius: 12px;
            padding: 1.5rem;
        }

        .interests-grid {
            display: flex;
            flex-wrap: wrap;
            gap: 0.75rem;
        }

        .interest-tag {
            background: var(--white);
            color: var(--secondary);
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-size: 0.875rem;
            box-shadow: var(--shadow);
        }

        .profile-actions {
            text-align: center;
            margin-top: 2rem;
        }

        .btn-primary {
            background: var(--primary);
            border: none;
            color: var(--secondary);
            padding: 1rem 2rem;
            border-radius: 12px;
            font-weight: 600;
            font-size: 1rem;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(238, 212, 126, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(238, 212, 126, 0.4);
            background: var(--gradient);
        }

        .btn-primary i {
            transition: transform 0.3s ease;
        }

        .btn-primary:hover i {
            transform: translateX(5px);
        }

        .alert-danger {
            background-color: #fff5f5;
            border: 1px solid #feb2b2;
            color: #c53030;
            padding: 1rem;
            border-radius: 12px;
            margin-bottom: 1.5rem;
        }

        @media (max-width: 991px) {
            .profile-grid {
                grid-template-columns: 1fr;
                gap: 2rem;
            }

            .profile-image-wrapper {
                width: 200px;
                height: 200px;
            }

            .details-grid {
                grid-template-columns: 1fr;
            }
        }

        @media (max-width: 768px) {
            .profile-container {
                padding: 2rem 1rem;
            }

            .profile-content {
                padding: 2rem;
            }

            .profile-header h1 {
                font-size: 2rem;
            }

            .profile-header p {
                font-size: 1rem;
            }
        }
    </style>

    <script src="/js/profile.js"></script>
    <script src="/js/bookCategories.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // The existing profile.js will handle populating the selected categories
            createBookCategoriesUI('interestedBooks');
        });
    </script>
    <%- include('partials/footer') %>