<!-- views/index.ejs -->
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BookSwap | Modern Book Sharing Platform</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #EED47E;
            --secondary: #2C3E50;
            --accent: #34495E;
            --light: #F8F9FA;
            --dark: #1A1A1A;
            --white: #FFFFFF;
            --text: #2C3E50;
            --gradient: linear-gradient(135deg, #EED47E, #F4E5A3);
            --shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            --card-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--white);
            color: var(--text);
            overflow-x: hidden;
            margin: 0;
            padding: 0;
            line-height: 1.6;
        }

        .hero {
            min-height: 100vh;
            background: var(--white);
            position: relative;
            display: flex;
            align-items: center;
            padding: 6rem 0;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 60%;
            height: 100%;
            background: var(--gradient);
            clip-path: polygon(20% 0%, 100% 0%, 100% 100%, 0% 100%);
            z-index: 1;
            opacity: 0.9;
        }

        .hero-content {
            position: relative;
            z-index: 2;
            max-width: 600px;
            margin-left: 10%;
            padding: 2rem;
        }

        .hero-image {
            position: absolute;
            right: 12%;
            top: 50%;
            transform: translateY(-50%);
            z-index: 2;
            width: 400px;
            height: 500px;
            border-radius: 30px;
            overflow: hidden;
            box-shadow: var(--card-shadow);
            transform-style: preserve-3d;
            perspective: 1000px;
        }

        .hero-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: all 0.5s ease;
            transform: rotateY(0deg);
        }

        .hero-image:hover img {
            transform: rotateY(5deg) scale(1.05);
        }

        .hero-content h1 {
            font-size: 4.5rem;
            font-weight: 800;
            margin-bottom: 1.5rem;
            color: var(--secondary);
            line-height: 1.1;
            letter-spacing: -0.02em;
            background: linear-gradient(45deg, var(--secondary), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero-content p {
            font-size: 1.35rem;
            color: var(--accent);
            margin-bottom: 2.5rem;
            font-weight: 400;
            opacity: 0.9;
            line-height: 1.6;
        }

        .hero-buttons {
            display: flex;
            gap: 1.5rem;
        }

        .feature-card {
            background: var(--white);
            border: none;
            border-radius: 20px;
            padding: 2.5rem;
            transition: all 0.4s ease;
            height: 100%;
            box-shadow: var(--shadow);
            position: relative;
            overflow: hidden;
            border: 1px solid rgba(238, 212, 126, 0.1);
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 4px;
            height: 100%;
            background: var(--gradient);
            opacity: 0;
            transition: opacity 0.4s ease;
        }

        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: var(--card-shadow);
            border-color: var(--primary);
        }

        .feature-card:hover::before {
            opacity: 1;
        }

        .feature-card i {
            color: var(--primary);
            font-size: 2.5rem;
            margin-bottom: 1.5rem;
            transition: transform 0.3s ease;
        }

        .feature-card:hover i {
            transform: scale(1.1);
        }

        .feature-card h3 {
            color: var(--secondary);
            font-weight: 700;
            margin-bottom: 1rem;
            font-size: 1.4rem;
        }

        .feature-card p {
            color: var(--accent);
            font-size: 1rem;
            opacity: 0.8;
            line-height: 1.6;
        }

        .how-it-works-step {
            background: var(--white);
            border: none;
            border-radius: 20px;
            padding: 2.5rem;
            text-align: left;
            transition: all 0.4s ease;
            height: 100%;
            box-shadow: var(--shadow);
            position: relative;
            border: 1px solid rgba(238, 212, 126, 0.1);
        }

        .how-it-works-step:hover {
            transform: translateY(-10px);
            box-shadow: var(--card-shadow);
            border-color: var(--primary);
        }

        .how-it-works-step i {
            font-size: 2.5rem;
            margin-bottom: 1.5rem;
            color: var(--primary);
            transition: transform 0.3s ease;
        }

        .how-it-works-step:hover i {
            transform: scale(1.1);
        }

        .how-it-works-step h3 {
            color: var(--secondary);
            font-weight: 700;
            margin-bottom: 1rem;
            font-size: 1.4rem;
        }

        .how-it-works-step p {
            color: var(--accent);
            font-size: 1rem;
            opacity: 0.8;
            line-height: 1.6;
        }

        .cta-banner {
            background: var(--secondary);
            border-radius: 30px;
            padding: 5rem;
            text-align: center;
            position: relative;
            overflow: hidden;
            margin: 6rem 0;
            box-shadow: var(--card-shadow);
        }

        .cta-banner::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(45deg, rgba(238, 212, 126, 0.1), transparent);
            z-index: 1;
        }

        .cta-banner h2 {
            font-size: 3rem;
            margin-bottom: 1.5rem;
            color: var(--white);
            font-weight: 800;
            position: relative;
            z-index: 2;
        }

        .cta-banner p {
            color: var(--white);
            font-size: 1.2rem;
            opacity: 0.9;
            margin-bottom: 2.5rem;
            position: relative;
            z-index: 2;
        }

        .footer {
            background: var(--secondary);
            padding: 5rem 0 2rem;
            color: var(--white);
            margin-top: 4rem;
        }

        .footer h4 {
            color: var(--white);
            font-weight: 700;
            margin-bottom: 1.5rem;
            font-size: 1.4rem;
        }

        .footer a {
            color: var(--white);
            text-decoration: none;
            transition: all 0.3s ease;
            opacity: 0.8;
            display: block;
            margin-bottom: 0.8rem;
            font-size: 1.1rem;
        }

        .footer a:hover {
            color: var(--primary);
            opacity: 1;
            transform: translateX(5px);
        }

        .btn-primary {
            background: var(--primary);
            border: none;
            color: var(--secondary);
            padding: 1rem 2.5rem;
            border-radius: 12px;
            transition: all 0.3s ease;
            font-weight: 600;
            font-size: 1.1rem;
            box-shadow: 0 4px 15px rgba(238, 212, 126, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(238, 212, 126, 0.4);
            background: var(--gradient);
        }

        .btn-outline-light {
            color: var(--secondary);
            border: 2px solid var(--secondary);
            padding: 1rem 2.5rem;
            border-radius: 12px;
            transition: all 0.3s ease;
            font-weight: 600;
            font-size: 1.1rem;
        }

        .btn-outline-light:hover {
            background: var(--secondary);
            border-color: var(--secondary);
            color: var(--white);
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(44, 62, 80, 0.2);
        }

        .section-title {
            text-align: center;
            margin-bottom: 5rem;
        }

        .section-title h2 {
            font-size: 3rem;
            margin-bottom: 1.5rem;
            color: var(--secondary);
            font-weight: 800;
            background: linear-gradient(45deg, var(--secondary), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .section-title p {
            color: var(--accent);
            font-size: 1.2rem;
            font-weight: 400;
            opacity: 0.8;
            max-width: 600px;
            margin: 0 auto;
        }

        @media (max-width: 1200px) {
            .hero-image {
                width: 350px;
                height: 450px;
                right: 8%;
            }
        }

        @media (max-width: 992px) {
            .hero-image {
                width: 300px;
                height: 400px;
                right: 5%;
            }
        }

        @media (max-width: 768px) {
            .hero {
                padding: 4rem 0;
            }

            .hero::before {
                width: 100%;
                height: 50%;
                clip-path: polygon(0 0, 100% 0, 100% 100%, 0 100%);
            }

            .hero-content {
                margin: 0;
                padding: 2rem;
                text-align: center;
            }

            .hero-image {
                position: relative;
                right: auto;
                top: auto;
                transform: none;
                width: 85%;
                height: 350px;
                margin: 2rem auto;
            }

            .hero-content h1 {
                font-size: 3rem;
            }

            .hero-content p {
                font-size: 1.2rem;
            }

            .hero-buttons {
                flex-direction: column;
                align-items: center;
            }

            .hero-buttons .btn {
                width: 100%;
                margin: 0.5rem 0;
            }

            .feature-card,
            .how-it-works-step {
                margin-bottom: 1.5rem;
            }

            .section-title h2 {
                font-size: 2.5rem;
            }

            .cta-banner {
                padding: 3rem;
                margin: 4rem 0;
            }

            .cta-banner h2 {
                font-size: 2.5rem;
            }
        }
    </style>
</head>

<body>
    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <h1>Share Books, Share Stories</h1>
            <p>Join our community of book lovers and discover new stories through the power of sharing.</p>
            <div class="hero-buttons">
                <a href="/signup" class="btn btn-primary">Get Started</a>
                <a href="/login" class="btn btn-outline-light">Sign In</a>
            </div>
        </div>
        <div class="hero-image">
            <img src="https://images.unsplash.com/photo-1543002588-bfa74002ed7e?auto=format&fit=crop&w=800&q=80"
                alt="Beautiful book">
        </div>
    </section>

    <!-- Features Section -->
    <section class="py-5">
        <div class="container">
            <div class="section-title">
                <h2>Why Choose BookSwap?</h2>
                <p>Experience the joy of sharing books with fellow readers</p>
            </div>
            <div class="row g-4">
                <div class="col-md-4">
                    <div class="feature-card">
                        <i class="fas fa-book-reader"></i>
                        <h3>Easy Exchange</h3>
                        <p>Swap books with other readers in your community with just a few clicks.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="feature-card">
                        <i class="fas fa-users"></i>
                        <h3>Vibrant Community</h3>
                        <p>Join a community of book lovers and share your reading journey.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="feature-card">
                        <i class="fas fa-shield-alt"></i>
                        <h3>Secure Platform</h3>
                        <p>Your data and transactions are protected with industry-standard security.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- How It Works Section -->
    <section class="py-5" style="background-color: var(--light);">
        <div class="container">
            <div class="section-title">
                <h2>How It Works</h2>
                <p>Get started with BookSwap in three simple steps</p>
            </div>
            <div class="row g-4">
                <div class="col-md-4">
                    <div class="how-it-works-step">
                        <i class="fas fa-user-plus"></i>
                        <h3>1. Create Account</h3>
                        <p>Sign up and create your profile in minutes.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="how-it-works-step">
                        <i class="fas fa-book"></i>
                        <h3>2. Add Books</h3>
                        <p>List the books you want to share with others.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="how-it-works-step">
                        <i class="fas fa-exchange-alt"></i>
                        <h3>3. Start Swapping</h3>
                        <p>Connect with other readers and exchange books.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="container">
        <div class="cta-banner">
            <h2>Ready to Start Your Reading Journey?</h2>
            <p>Join thousands of readers who are already sharing books on BookSwap.</p>
            <a href="/signup" class="btn btn-primary">Get Started Now</a>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    <h4>BookSwap</h4>
                    <p>Share books, share knowledge, share joy.</p>
                </div>
                <div class="col-md-4">
                    <h4>Quick Links</h4>
                    <ul class="list-unstyled">
                        <li><a href="/about">About Us</a></li>
                        <li><a href="/contact">Contact</a></li>
                        <li><a href="/faq">FAQ</a></li>
                    </ul>
                </div>
                <div class="col-md-4">
                    <h4>Connect With Us</h4>
                    <div class="social-links">
                        <a href="#" class="me-3"><i class="fab fa-facebook"></i></a>
                        <a href="#" class="me-3"><i class="fab fa-twitter"></i></a>
                        <a href="#" class="me-3"><i class="fab fa-instagram"></i></a>
                    </div>
                </div>
            </div>
            <hr class="mt-4" style="border-color: rgba(255, 255, 255, 0.1);">
            <div class="text-center">
                <p>&copy; 2024 BookSwap. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>